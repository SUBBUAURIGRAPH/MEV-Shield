FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Set environment for admin dashboard
ENV REACT_APP_DASHBOARD_TYPE=admin
ENV PORT=3001

# Expose port
EXPOSE 3001

# Start the admin dashboard
CMD ["npm", "run", "start:admin"]